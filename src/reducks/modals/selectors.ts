import { ModalsAlias } from "./types";

export const getStageClearModalState = (modals: ModalsAlias) => modals.stageClearModal;

export const getRightOrWrongModalState = (modals: ModalsAlias) => modals.rightOrWrongModal;
